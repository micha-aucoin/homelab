---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: nfs-subdir-external-provisioner
spec:
  chart:
    spec:
      version: "*"
  values:
    nfs:
      server: "nas-service"
      path: "/volume1/k8s-data-test"
