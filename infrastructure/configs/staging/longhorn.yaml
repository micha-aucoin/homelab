# ---
# apiVersion: networking.k8s.io/v1
# kind: Ingress
# metadata:
#   name: ts-longhorn
#   namespace: longhorn-system
# spec:
#   defaultBackend:
#     service:
#       name: longhorn-frontend
#       port:
#         number: 80
#   ingressClassName: tailscale
#   tls:
#     - hosts:
#         - longhorn-frontend
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: longhorn-ingress
  namespace: longhorn-system
spec:
  ingressClassName: traefik
  rules:
  - host: longhorn-s.boxadvisers.com
    http:
      paths:
      - backend:
          service:
            name: longhorn-frontend
            port:
              number: 80
        path: /
        pathType: Prefix
